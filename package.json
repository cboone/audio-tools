{
  "name": "audio-tools-lint",
  "version": "0.0.0",
  "private": true,
  "description": "Pinned text-linting tools for audio-tools. Not a JavaScript project: these are the Markdown, formatting and spelling linters, pinned here so local runs and CI use the same versions.",
  "license": "MIT",
  "scripts": {
    "lint:md": "markdownlint-cli2 \"**/*.md\"",
    "lint:md:fix": "markdownlint-cli2 --fix \"**/*.md\"",
    "format:check": "prettier --check .",
    "format:write": "prettier --write .",
    "spell": "cspell --no-progress ."
  },
  "devDependencies": {
    "cspell": "10.1.1",
    "markdownlint-cli2": "0.23.2",
    "prettier": "3.9.6"
  }
}
